/**:
  ros__parameters:
    robot_description_planning:
      cartesian_limits:
        # Placeholder values — tune to the real weld travel speed before production use.
        max_trans_vel: 0.05      # m/s
        max_trans_acc: 0.1       # m/s^2
        max_trans_dec: -0.1      # m/s^2
        max_rot_vel: 0.5         # rad/s
